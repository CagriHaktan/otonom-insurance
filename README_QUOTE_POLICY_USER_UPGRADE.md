# Otonom Insurance Quote + Policy + User Upgrade

Features:
- Clickable insurance type cards
- Type-based quote page
- Customer information form
- Random offer price generation
- Offer accept and policy process
- Login page no longer shows demo passwords
- Register system
- Admin can see registered users
- Admin can manage quotation and policy creation

Apply:
1. Copy `frontend/src` into existing `frontend/src`
2. Copy `backend/src` into existing `backend/src`
3. Restart backend and frontend

Commit:
Improve quotation, policy and user management system
