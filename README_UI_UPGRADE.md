# Otonom Insurance UI Upgrade v2

Copy the `frontend/src` folder into your existing project and replace the old files.

Then run:

```bash
cd frontend
npm run dev
```

Git commit message:

```text
Improve user interface design
```
